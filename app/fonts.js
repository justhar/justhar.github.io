// or similar file where fonts are imported
import { Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

// Remove any other font imports and exports
